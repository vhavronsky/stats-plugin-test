extends:
  - recommended

apis:
  main:
    root: ./openapi.yaml
  secondary:
    root: ./openapi-secondary.yaml

stats:
  fileExtensions: true

navbar:
  items:
    - page: /index.md
      label: Home

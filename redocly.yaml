extends:
  - recommended

apis:
  main:
    root: ./openapi.yaml
  secondary:
    root: ./openapi-secondary.yaml
  project:
    root: ./docs/project-delivery.yaml

stats:
  fileExtensions: true
  apis:
    - name: test api
      path: ./openapi.yaml

navbar:
  items:
    - page: /index.md
      label: Home

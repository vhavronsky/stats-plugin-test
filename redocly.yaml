extends:
  - recommended

apis:
  main:
    root: ./openapi.yaml
  secondary:
    root: ./openapi-secondary.yaml

stats:
  fileExtensions: true
  apis:
    - name: primary-custom-name
      path: ./openapi.yaml

navbar:
  items:
    - page: /index.md
      label: Home

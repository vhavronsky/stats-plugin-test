extends:
  - recommended

apis:
  main:
    root: ./openapi.yaml
  secondary:
    root: ./openapi-secondary.yaml
  project:
    root: ./docs/project-delivery.yaml

stats:
  fileExtensions: true

navbar:
  items:
    - page: /index.md
      label: Home

requiresLogin: true

residency: https://app.lab6.blueharvest.cloud
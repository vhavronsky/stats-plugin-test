extends:
  - recommended

apis:
  main:
    root: ./openapi.yaml
  secondary:
    root: ./openapi-secondary.yaml

stats:
  fileExtensions: true
  apis:
    - name: missing-api
      path: ./does-not-exist.yaml

navbar:
  items:
    - page: /index.md
      label: Home
